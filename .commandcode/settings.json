{
  "permissions": {
    "allow": [
      "Shell(node -c controllers/categoryGroupController.js)",
      "Shell(node -c models/categoryGroupModel.js)",
      "Shell(flutter:*)",
      "Shell(cat:*)",
      "Shell(grep:*)",
      "Shell(agent-browser --version 2>&1; which agent-browser)",
      "Shell(pgrep:*)",
      "Shell(ss:*)",
      "Shell(npm:*)",
      "Shell(curl -s -o /dev/null -w \"GET /api/category-groups (anon) -> %{http_code}\\n\" http://localhost:6900/api/category-groups curl -s -o /dev/null -w \"GET /view-category-groups (admin, no auth) -> %{http_code}\\n\" http://localhost:6900/view-category-groups curl -s -o /dev/null -w \"GET /add-category-group (admin, no auth) -> %{http_code}\\n\" http://localhost:6900/add-category-group curl -s -o /dev/null -w \"GET / (root) -> %{http_code}\\n\" http://localhost:6900/)",
      "Shell(head:*)",
      "Shell(ps:*)",
      "Shell(node -e \" const ejs = require('ejs'); const fs = require('fs'); // Stub the includes for syntax-only parse const stubLayout = '<% /* header */ %><%= title %><% /* footer */ %>'; const tmpls = ['addCategoryGroup', 'editCategoryGroup', 'viewCategoryGroups']; for (const t of tmpls) { const path = './views/admin/' + t + '.ejs'; const src = fs.readFileSync(path, 'utf8'); // Replace includes with a no-op include function const stripped = src.replace(/<%- include\\([^)]+\\) %>/g, ''); try { ejs.compile(stripped, { filename: path }); console.log('OK ', t, 'parses'); } catch (e) { console.log('FAIL', t, e.message); } } \")",
      "Shell(node -e \" // Simulate the controller's normalize function const allowed = ['central', 'state', 'none']; const normalizeScope = (value) => allowed.includes(value) ? value : 'none'; const tests = [ ['central', 'central'], ['state', 'state'], ['none', 'none'], [undefined, 'none'], [null, 'none'], ['', 'none'], ['garbage', 'none'], ['CENTRAL', 'none'], ['Central', 'none'], ]; let pass = 0, fail = 0; for (const [input, expected] of tests) { const got = normalizeScope(input); if (got === expected) { pass++; } else { fail++; console.log('FAIL', JSON.stringify(input), '->', got, 'expected', expected); } } console.log('normalizeScope:', pass + '/' + (pass+fail), 'passed'); \")",
      "Shell(node -e \" const ejs = require('ejs'); const fs = require('fs'); // Render the templates with mock legacy data (no scope field) const tpl = fs.readFileSync('./views/admin/viewCategoryGroups.ejs', 'utf8'); const out = ejs.render(tpl, { groups: [ { _id: 'a', displayName: 'Railway', categories: [{_id:'c1', displayName:'RRB', name:'RRB'}] }, // no scope { _id: 'b', displayName: 'State PSC', scope: 'state', categories: [] }, // has scope { _id: 'c', displayName: 'SSC', scope: 'central', categories: [] }, { _id: 'd', displayName: 'Bogus', scope: 'garbage', categories: [] }, // invalid scope ] }, { filename: './views/admin/viewCategoryGroups.ejs' }); // Look for the rendered badges const checks = [ ['Legacy (no scope) shows None badge', out.includes('>None<')], ['State shows State wise badge', out.includes('>State wise<')], ['Central shows Central wise badge', out.includes('>Central wise<')], ['Garbage scope falls back to None', /Bogus[\\s\\S]{0,200}None/.test(out)], ['No raw undefined string in output', !out.includes('undefined')], ['Bootstrap badge classes present', out.includes('badge bg-primary') && out.includes('badge bg-success')], ]; for (const [label, ok] of checks) { console.log((ok ? 'OK ' : 'FAIL') + ' ' + label); } \")",
      "Shell(node -c scripts/backfillCategoryGroupScope.js)",
      "Shell(node -e \" // Reuse the classifier logic to sanity-check against expected mappings const CENTRAL = ['railway', 'ssc', 'psu', 'banking', 'upsc', 'ibps', 'sbi', 'rbi', 'central', 'rrb', 'defence', 'defense', 'navy', 'army', 'air force', 'capf', 'cpo', 'cgl', 'chsl', 'mts', 'ntpc', 'lic', 'fci']; const STATE = ['state', 'psc', 'state wise', 'statewise', 'tnpsc', 'appsc', 'kpsc', 'uppsc', 'mppsc', 'bpsc', 'rpsc', 'hpsc', 'spsc', 'wbcs', 'ppsc', 'gpsc', 'mpsc', 'jpsc', 'cgpsc', 'apspsc']; function classify(name) { const n = String(name || '').toLowerCase().trim(); if (!n) return 'none'; for (const t of CENTRAL) if (n.includes(t)) return 'central'; for (const t of STATE) if (n.includes(t)) return 'state'; return 'none'; } const cases = [ // [input, expected] ['Railway', 'central'], ['SSC CGL', 'central'], ['PSU', 'central'], ['RRB NTPC', 'central'], ['UPSC', 'central'], ['Banking', 'central'], ['IBPS PO', 'central'], ['State PSC', 'state'], ['TNPSC Group 1', 'state'], ['APPSC', 'state'], ['UPPSC', 'state'], ['State Wise Exams', 'state'], ['Current Affairs', 'none'], ['Mock Tests', 'none'], ['Quizzes', 'none'], ['', 'none'], [null, 'none'], [undefined, 'none'], ['Ministry of Railways (State Board)', 'central'], // 'railway' matched first ['State Bank of India', 'central'], // 'sbi' matched first (central) ]; let pass = 0, fail = 0; for (const [input, expected] of cases) { const got = classify(input); const ok = got === expected; if (ok) pass++; else fail++; console.log((ok ? 'OK ' : 'FAIL') + ' ' + JSON.stringify(input).padEnd(28) + ' -> ' + got + (ok ? '' : ' (expected ' + expected + ')')); } console.log('\\n' + pass + '/' + (pass + fail) + ' passed'); \")",
      "Shell(node -e \" const CENTRAL = ['railway','ssc','psu','banking','upsc','ibps','sbi','rbi','central','rrb','defence','defense','navy','army','air force','capf','cpo','cgl','chsl','mts','ntpc','lic','fci']; const STATE = ['state','psc','state wise','statewise','tnpsc','appsc','kpsc','uppsc','mppsc','bpsc','rpsc','hpsc','spsc','wbcs','ppsc','gpsc','mpsc','jpsc','cgpsc','apspsc']; function classify(name) { const n = String(name || '').toLowerCase().trim(); if (!n) return 'none'; for (const t of CENTRAL) if (n.includes(t)) return 'central'; for (const t of STATE) if (n.includes(t)) return 'state'; return 'none'; } const cases = [ ['Ministry of Railways (State Board)', 'central'], ['State Bank of India', 'central'], ]; for (const [input, expected] of cases) { const got = classify(input); console.log((got === expected ? 'OK ' : 'FAIL') + ' ' + input.padEnd(32) + ' -> ' + got + (got === expected ? '' : ' (expected ' + expected + ')')); } \")",
      "Shell(echo:*)",
      "Shell(node -e \" // Verify 'State Bank of India' really doesn't contain 'sbi' as a substring const s = 'state bank of india'; console.log('lowercased:', s); console.log('contains sbi:', s.includes('sbi')); console.log('contains bank:', s.includes('bank')); console.log('contains ibps:', s.includes('ibps')); \")",
      "Shell(rm:*)",
      "Shell(node scripts/listQuizzes.js)",
      "Shell(node scripts/seedSubjectWiseQuestions.js)",
      "Shell(node -e  require('dotenv').config(); const mongoose = require('mongoose'); mongoose.connect(process.env.DB_CONNECTION).then(async () => { const Quiz = require('./models/quizModel'); const q = await Quiz.findOne({name: 'Demo Subject Wise - Chapter Test (20)'}); console.log('Quiz:', q.name); console.log('correct_ans_reward_per_question:', q.correct_ans_reward_per_question); console.log('penalty_per_question:', q.penalty_per_question); console.log('total_questions:', q.total_questions); mongoose.disconnect(); }); )",
      "Shell(node -e  require('dotenv').config(); const mongoose = require('mongoose'); mongoose.connect(process.env.DB_CONNECTION).then(async () => { const UserQuiz = require('./models/userQuizModel'); const Quiz = require('./models/quizModel'); const q = await Quiz.findOne({name: 'Demo Subject Wise - Chapter Test (20)'}); const results = await UserQuiz.find({quizId: q._id}).select('userId score correct_answers wrong_answers total_questions'); console.log('Results for this quiz:'); results.forEach(r => console.log(JSON.stringify(r))); mongoose.disconnect(); }); )",
      "Shell(node -e  require('dotenv').config(); const mongoose = require('mongoose'); mongoose.connect(process.env.DB_CONNECTION).then(async () => { const UserQuiz = require('./models/userQuizModel'); const Quiz = require('./models/quizModel'); const q = await Quiz.findOne({name: 'Demo Subject Wise - Chapter Test (20)'}); console.log('Quiz ID:', q._id); const count = await UserQuiz.countDocuments({quizId: q._id}); console.log('Total records:', count); if (count > 0) { const sample = await UserQuiz.findOne({quizId: q._id}).lean(); console.log('Sample record:', JSON.stringify(sample, null, 2)); } mongoose.disconnect(); }); )",
      "Shell(node -e  require('dotenv').config(); const mongoose = require('mongoose'); mongoose.connect(process.env.DB_CONNECTION).then(async () => { const UserQuiz = require('./models/userQuizModel'); const count = await UserQuiz.countDocuments(); console.log('Total UserQuiz records:', count); if (count > 0) { const samples = await UserQuiz.find().sort({createdAt: -1}).limit(3).populate('quizId', 'name').lean(); samples.forEach(s => console.log('Quiz:', s.quizId?.name, '| score:', s.score, '| correct:', s.correct_answers, '| total:', s.total_questions)); } mongoose.disconnect(); }); )",
      "Shell(node -e  require('dotenv').config(); const mongoose = require('mongoose'); mongoose.connect(process.env.DB_CONNECTION).then(async () => { const UserQuiz = require('./models/userQuizModel'); const Quiz = require('./models/quizModel'); const samples = await UserQuiz.find().sort({createdAt: -1}).limit(5).lean(); for (const s of samples) { const q = await Quiz.findById(s.quizId).lean(); console.log('Quiz:', q?.name || 'UNKNOWN', '| score:', s.score, '| correct:', s.correct_answers, '| wrong:', s.wrong_answers, '| total:', s.total_questions); } mongoose.disconnect(); }); )",
      "Shell(node -e  require('dotenv').config(); const mongoose = require('mongoose'); mongoose.connect(process.env.DB_CONNECTION).then(async () => { const UserQuiz = require('./models/userQuizModel'); const Quiz = require('./models/quizModel'); const q = await Quiz.findOne({name: 'Demo Subject Wise - Chapter Test (20)'}); const results = await UserQuiz.find({quizId: q._id}).lean(); console.log('Records for this quiz:', results.length); if (results.length > 0) { results.forEach(r => console.log('score:', r.score, '| correct:', r.correct_answers, '| wrong:', r.wrong_answers, '| total:', r.total_questions)); } else { console.log('NO RECORDS - startquiz API was never called for this quiz'); } mongoose.disconnect(); }); )",
      "Shell(node -e  require('dotenv').config(); const mongoose = require('mongoose'); mongoose.connect(process.env.DB_CONNECTION).then(async () => { const UserQuiz = require('./models/userQuizModel'); const Quiz = require('./models/quizModel'); const results = await UserQuiz.find().sort({createdAt: -1}).limit(5).lean(); for (const r of results) { const q = await Quiz.findById(r.quizId).lean(); console.log('Quiz:', q?.name || 'UNKNOWN', '| score:', r.score, '| correct:', r.correct_answers, '| total:', r.total_questions, '| date:', r.createdAt); } mongoose.disconnect(); }); )",
      "Shell(node -e  require('dotenv').config(); const mongoose = require('mongoose'); mongoose.connect(process.env.DB_CONNECTION).then(async () => { const Quiz = require('./models/quizModel'); const quizzes = await Quiz.find({name: {: /Demo|demo/i}}).lean(); quizzes.forEach(q => console.log(q._id, '|', q.name)); mongoose.disconnect(); }); )",
      "Shell(node -e  require('dotenv').config(); const mongoose = require('mongoose'); mongoose.connect(process.env.DB_CONNECTION).then(async () => { const Quiz = require('./models/quizModel'); const quizzes = await Quiz.find({name: /demo/i}).lean(); quizzes.forEach(q => console.log(q._id.toString(), '|', q.name)); mongoose.disconnect(); }); )"
    ],
    "deny": [],
    "defaultMode": "default"
  }
}